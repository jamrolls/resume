An optional "Highlighted Achievements" section can now be added under
the summary, via a new top-level `highlights` object with a `bullets`
array (and, like work experience entries, an optional `class` for
styling).
