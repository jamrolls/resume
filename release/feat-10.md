An optional "Key Achievements" label and bullet list can now be added
under the summary, via a new top-level `highlights` object with a
`bullets` array (and, like work experience entries, an optional
`class` for styling). It's rendered as styled text rather than a
heading, so it stays part of the summary/objective for ATS parsers.
